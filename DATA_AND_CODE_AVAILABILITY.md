# Data and code availability

The fixed partition IDs, derived per-page metrics, configurations, and analysis code are included. Dataset images and pretrained weights are excluded because they are governed by third-party licenses. The official RTM test set was not accessed.
